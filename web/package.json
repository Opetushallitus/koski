{
  "name": "tor",
  "version": "0.0.1",
  "description": "Todennetun osaamisen rekisteri",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "EUPL-1.1",
  "dependencies": {
    "react": "~0.13",
    "axios": "~0.5",
    "baconjs": "~0.7"
  },
  "devDependencies": {
    "nodemon": "^1",
    "webpack": "^1",
    "babel": "^5",
    "babel-core": "^5",
    "babel-loader": "^5"
  },
  "scripts": {
    "postinstall": "webpack -p",
    "watch": "nodemon --quiet --watch app --watch package.json -e jsx --exec 'npm install'"
  }
}
