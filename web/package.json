{
  "name": "tor",
  "version": "0.0.1",
  "description": "Todennetun osaamisen rekisteri",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "EUPL-1.1",
  "devDependencies": {
    "nodemon": "^1.4",
    "webpack": "^1.10.5"
  },
  "scripts": {
    "postinstall": "webpack -p",
    "watch": "nodemon --quiet --watch app -e jsx --exec 'npm install'"
  }
}
